Build dependency `embed-manifest` is used for windows build

ease `clap` version restriction

remove build="build.rs"

add `rm build.rs` in %prep


disable test temp

maybe due to clone3 in docker
error: 5 targets failed:
    `--test cli_surface`
    `--test internal_daemon`
    `--test issue8_interactive_exit`
    `--test manpage_surface`
    `--test stress`

